# Honor Code

## 完成作业参考的内容

- 对于导入的候选词库/可用词库，判断其中是否有重复元素这一问题，我开始使用的是Vec和Vec的contains函数，发现测试超时，经过与同学交流发现使用Vec的contains函数时间复杂度较高，而使用HashSet可以完成更高效本任务，故修改了容器的使用

- 对于交互模式下如何打印出字母表状态这一问题，我和同学展开了讨论，我开始提出直接按照字母表顺序打印，同学想到可以仿照wordle原游戏下的键盘打印，并且在讨论后认为可以先将键盘按每行存到容器中，在遍历查找每个字母的状态进行打印，最后实现了更加还原原游戏的效果

- 关于在tui中如何调整输出框大小的问题，我向 ChatGPT 提问了：怎么调整tui库创建的输出框的大小？ChatGPT回答了：要调整输出框的大小，您可以按照以下步骤操作：
1. 修改布局：将Layout的约束数组中添加一个新的Constraint::Percentage，用于表示输出框所占的高度。例如，您可以使用Constraint::Percentage(50)表示输出框占据屏幕的50%高度。
2. 添加输出逻辑：在绘制UI时，使用新的约束将输出框渲染到相应的位置上。
3. 更新事件处理：根据需要，处理键盘事件，并将相应的输出写入输出框。

这启发了我的tui窗口设计

- 关于如何检查导入词库的合法性，我向ChatGPT提问了：rust中怎么检查一个字符串是不是五个字母组成的单词，请只给出思路不要给出代码。ChatGPT回答了：在Rust中，你可以按照以下思路来检查一个字符串是否由五个字母组成的单词：

1. 使用字符串的chars()方法遍历每个字符。
2. 对于每个字符，使用is_alphabetic()方法检查它是否是一个字母。
3. 统计字符串中字符的数量，可以使用字符串的len()方法。
4. 检查字符数量是否为5。
5. 综合考虑以上条件，判断字符串是否满足五个字母组成的单词的条件。
6. 通过按照上述思路遍历和检查字符串的字符，你可以判断该字符串是否满足特定的条件，即由五个字母组成的单词。

如果你需要进一步实现这个逻辑，可以使用Rust中字符串和字符相关的方法和函数来操作和检查字符串。

这启发了我的检查方式

- 关于如何使用rayon库加速遍历，我向ChatGPT提问了：rust怎么使用rayon库加速数组的遍历，请只给出思路不要给出代码。ChatGPT回答了：要使用rayon库加速数组的遍历，你可以按照以下思路：

1. 导入rayon库的预lude，使其可以使用并行迭代的功能
2. 将要遍历的数组转换为Rayon所支持的并行迭代的类型，如Vec或&[T]。
3. 使用par_iter()方法并行迭代处理数组的元素。
4. 在每个并行迭代的元素上执行所需的操作。
5. 对于需要汇总结果的情况，可以使用.reduce()或.collect()等方法来将并行处理的结果合并。

通过以上的步骤，你可以利用rayon库的并行能力来加速数组的遍历。但要注意，在具体的实现中，还需要考虑并发访问共享数据的安全性和正确性。

这启发了我对rayon的使用

- 关于json库的解析，我向ChatGPT提问了：怎么将一个用json库解析出的value赋值给一个Vec<(String, Vec)>，请提供思路不要提供代码。ChatGPT回答了：要将由 JSON 库解析出的 Value 赋值给 Vec<(String, Vec)>，您可以按照以下思路进行操作：

1. 确定 Value 的类型，通常为 Object 或 Array。如果 Value 的类型不匹配，可能需要进行类型转换或错误处理。
2. 根据 Value 的类型，使用模式匹配或条件语句来访问所需的字段和数据。
3. 将访问到的数据转换为目标类型，并将其存储到 Vec<(String, Vec)> 中。

请注意，具体的实现方法可能因您的代码结构和数据格式而有所不同。这些步骤可以作为实现的指导，但具体的代码实现将根据您的需求和上下文而有所不同。

这启发了我对于json库的处理